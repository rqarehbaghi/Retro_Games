stable-retro
gymnasium
numpy
stable-baselines3[extra]

# Needed for train.py's frame preprocessing (grayscale/resize). Headless
# build -- no GUI libs needed, matches the rest of this headless setup.
opencv-python-headless

# GPU video encoding (NVENC) is NOT a pip package -- it comes from the NVIDIA
# driver and ffmpeg. `bash tools/check_nvenc.sh` (run by setup.sh) tests it and
# explains any fix. Renders use libx264 by default: on a WSL2 laptop NVENC
# measured slower than x264 and failed 3 of 18 encodes (see overlays.py).

# Optional -- only for `studio.py --writer claude`, which has the Anthropic
# API write the captions, commentary and descriptions. The `ollama` backend
# needs nothing beyond the standard library.
anthropic

# Optional -- only for `studio.py --voice`, which speaks the narration with
# Qwen3-TTS. Apache 2.0, about 4GB of weights, needs a CUDA GPU. It pulls its
# own torch, so install it in a separate venv if it disturbs the pinned one
# training depends on.
# qwen-tts
# soundfile
